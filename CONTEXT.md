# 三火工作台

个人商单管理与创作辅助工作台；灵感库用于沉淀可追溯的视觉参考与可复用的修图提示词。

## 语言术语

### 商单排期

**日历待办（Calendar To-do）**：按订单时间归属日期、且订单状态为“待办”的商单日历标记；未排期订单不产生标记，日期本身不显示待办数量。
_避免使用_：全部待处理商单、未完成商单

**日历浏览月（Calendar Viewing Month）**：首页日历当前展示的自然月；初始值为系统当前月，用户可临时切换上、下月，刷新后恢复为系统当前月。
_避免使用_：筛选月份、保存月份

**日历待办列表（Calendar To-do List）**：用户点击带有日历待办的日期后，以弹窗展示该日期内所有日历待办的只读列表，并可跳转至待办页。
_避免使用_：日程详情、全部订单列表

**日历当日（Calendar Today）**：与系统当天相同的日历日期；以主题绿色背景高亮，即使当天同时存在日历待办也保留该背景。
_避免使用_：当前查看月的第一天、选中日期

**首页待办预览（Home To-do Preview）**：首页原有的最近待办商单卡片；日历待办卡片启用后暂时隐藏，但不删除。
_避免使用_：日历待办卡片

**灵感模板（Inspiration Template）**：基于一张可追溯的真实参考封面图，经视觉解读得到的修图提示词条目。
_避免使用_：虚构模板、通用文案

**来源记录（Source Record）**：标识已下载为灵感模板封面的参考图之原始发布地址与发布平台的信息。
_避免使用_：图片出处、素材来源

## 关系

- 一个 **日历待办** 对应且仅对应一份状态为“待办”的订单；一份状态为“待办”的订单按其订单时间归属至一个日历日期
- 一个 **日历浏览月** 包含零个或多个 **日历待办**
- 一个 **日历待办列表** 展示一个日历日期内的零个或多个 **日历待办**
- 一个 **日历当日** 可同时是一个包含日历待办的日期
- 一个 **灵感模板** 基于且仅基于一份 **来源记录**
- 一份 **来源记录** 可被多个 **灵感模板** 分析使用

## 示例对话

> **开发者：** “这条食物提示词可以没有参考图吗？”
> **领域专家：** “不可以；每个 **灵感模板** 都必须能回到一份 **来源记录**。”

## 已标记的歧义

- 社交平台图片的下载与内置范围尚未明确：仅限用户拥有或获授权的内容，还是可自动收集公开内容。
